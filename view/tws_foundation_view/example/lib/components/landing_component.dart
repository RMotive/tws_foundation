import 'package:flutter/material.dart';

final class LandingComponent extends StatelessWidget {
  const LandingComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Welcome to TWS Foundation View package landing.'),
    );
  }
}
